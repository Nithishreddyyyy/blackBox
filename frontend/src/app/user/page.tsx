"use client";

/**
 * User home — placeholder for Part 2 (Chat interface).
 */
export default function UserPage() {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "var(--text-muted)",
        fontSize: 14,
      }}
    >
      Chat interface — coming in Part 2
    </div>
  );
}
